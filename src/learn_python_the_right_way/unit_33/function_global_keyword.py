##################################################
# 전역 범위(global scope)

# x = 10  # 전역 변수(global variable)


def foo():
    ##################################################
    # 지역 범위(local scope)
    global x  # 전역 변수 x 를 사용하겠다고 설정(전역에 없으면 전역 변수로 만듦)
    x = 20    # 전역 변수 x
    print(x)  # 전역 변수 출력
    #
    ##################################################


foo()
print(x)  # 전역 변수 출력
#
##################################################
